#include <stdlib.h>
#include <string.h>
#include <math.h>
#include "compressao.h" //inclui os Protótipos
#include "freq_w.h"

void compress(FILE *f){
        int *p;
        int i;
        p = freq_w(f);
        for( i = 0; i <= 255; i++){
            printf("LETRA: %d\n",);
        }
}
